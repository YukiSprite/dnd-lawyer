# ============================================
# API 配置示例文件
# ============================================
# 
# 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 填写您的实际配置信息
# 3. .env 文件不会被 git 跟踪（已在 .gitignore 中）
#

# API Key（必填）
API_KEY=your-api-key-here

# API 提供商类型（必填）
# "google" - Google 官方 Gemini API (gRPC)
# "openai" - OpenAI 兼容格式 (REST API)
API_PROVIDER=openai

# API Base URL（OpenAI 模式时必填）
API_BASE_URL=

# 模型名称（必填）
MODEL_NAME=gemini-2.5-pro

# 温度参数（可选，默认 0.1）
TEMPERATURE=0.1

# Embedding 模型名称（可选，默认 BAAI/bge-base-zh-v1.5）
EMBEDDING_MODEL_NAME=BAAI/bge-base-zh-v1.5

# Embedding 设备（可选，默认 cuda）
# 可选值: cuda, mps, cpu, auto
EMBEDDING_DEVICE=cuda

# Embedding 批处理大小
# 使用动态策略：从此值开始，失败则自动减半重试
EMBEDDING_BATCH_SIZE=512

# 多 GPU 支持（可选，默认 false）
# 如果有多块 GPU，设置为 true 可并行处理
EMBEDDING_MULTI_GPU=false
